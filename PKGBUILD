# PKGBUILD - Meta Arch

pkgname=meta-arch
pkgver=0
pkgrel=1
pkgdesc="Custom set of programs for Arch"
arch=('any')
license=('MIT')
depends=(
	base
	base-devel
	linux
	linux-firmware
	intel-ucode
	refind
	connman
	iwd
	zsh
)

package() {
	mkdir -p "$pkgdir"
}
